/**
 * Sample idle/talking clips generated for the Resse.ai receptionist POC
 * (see talkingpoc/prompt for the generation prompts). Swap these for
 * production-quality clips once available — same 9:16 framing/posture.
 */
export const defaultIdleSource = require("../assets/idle.mp4");
export const defaultTalkingSource = require("../assets/talking.mp4");
